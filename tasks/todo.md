# Portfolio site build — todo

Mirrors the build order in the Claude Code build prompt. Stop for review after each step.

- [x] Step 1 — Scaffold + content model: repo, CLAUDE.md, `data/cases.js` with all 10 cases (structural fields filled from the WRD; narrative blocks `[NEEDS INPUT]` — see list at bottom of this file)
- [x] Step 2 — Design tokens: `css/tokens.css` (palette, type scale, spacing, elevation), real subsetted font files in `fonts/`
- [x] Step 3 — Home page + Coverage Matrix (F1, F2, F3, F8, F13), mobile card list first — see review section below
- [x] Step 4 — `/work` with URL-encoded filters (F4, F5, F6) — see review section below
- [x] Step 5 — Case study pages generated by `scripts/build-case-pages.mjs`, with a hard publish guard — see review section below
- [x] Step 6 — `/about`, `/contact`, `/method`, SEO + JSON-LD (N7) — see review section below. Blocker: CV PDF still missing.
- [x] Step 7 — Audit pass: Lighthouse mobile, keyboard-only, JS-disabled, 375px check — see review section below
- [x] Step 8 — Landing page rebuild: Coverage Matrix retired, new static hero/about/work/credentials/close page, pastel palette, portrait placeholder — see review section below
- [x] Step 9 — Unify visual system: `/work`, case pages, `/about`, `/contact`, `/method` brought onto the landing page's palette/spacing/component language; found and fixed a real CLS regression along the way (missing font preloads on every page but home) — see review section below
- [x] Step 10 — Railway deploy prep (config only, not connected/deployed): `--allow-drafts` flag, Dockerfile + Caddyfile, `railway.json`, `404.html`, README deploy steps — see review section below

## Open items flagged during Step 1

- Domain-label mismatches between the WRD's Coverage Matrix (§4.1) and the case inventory (§6.1): C4 is labelled "Infrastructure & community" in §6.1 but mapped to the canonical "Infrastructure & policy" row here; C6 is labelled "Infrastructure / web" and mapped to "Developer / web infrastructure." Worth Ayomide's eyes to confirm these are the right calls.
- C9 bundles two engagements (HostMeNG's Clea/Pushbio work, and Calnita's beauty MVPs) into a single case per the WRD table — flagged in open question 6 of the WRD as possibly needing a split.
- No CV text has been supplied in this session — only the WRD. Every case's situation/missing/built/decisions/outcome/artifacts block is `[NEEDS INPUT]` because the WRD gives one-line summaries, not full narratives, per rule R1 in this project's CLAUDE.md.

## Matrix cell cross-check (§4.1 vs §6.1), run at Ayomide's request

Every case's domain + capabilities were checked against every filled cell in the §4.1 Coverage Matrix. Four filled cells have no case behind them under the current best-fit domain mapping — meaning, as built today, clicking those cells would have nothing to open, which breaks F1 ("filled cells are links"). None of these were fixed by adding a capability tag to a case, since the WRD's one-line case description doesn't support that tag — that would be inventing a claim, not just filling a gap.

1. **Civic & elections × Research & reporting** — C1 is the only civic case; its tagged capabilities (live ops, build from zero, stakeholder) don't include research.
2. **Events & community × Build from zero** — C5 is the only events case; its tagged capabilities (live ops, stakeholder) don't include build from zero.
3. **Developer/web infrastructure × Stakeholder/vendor** — C6 and C9 are the only devinfra cases; neither is tagged stakeholder.
4. **Consumer & marketplaces × Research & reporting** — C7 and C10 are the only consumer cases; neither is tagged research. (C9 is the one case with a research tag near this domain, but §6.1 gives it a dual label, "Developer infra / consumer," and it's mapped to devinfra here, not consumer — so it can't cover both cells with a single `domain` field.)

**Resolved, per Ayomide's ruling that §4.1 was her own inference and the data file wins by default:**

1. **Civic & elections × Research & reporting** — resolved. C1's real discovery content supports `research` (collation-readiness testing, integration testing against live uploads, a standing reporting cadence to the Lead), and Ayomide added the tag herself when she sent C1's real content.
2. **Events & community × Build from zero** — holding. Ayomide is checking her own records before tagging it — an unearned tag on the matrix is exactly what an interviewer pulls on. Left untagged; C5 stays without `build-from-zero` until she confirms.
3. **Developer/web infrastructure × Stakeholder/vendor** — dropped. HostMeNG's CV line describes collaborating with stakeholders, which is participation, not the stakeholder/vendor *management* capability. The WRD's §4.1 checkmark here was wrong; no case should carry this tag. No data change needed (none currently claims it).
4. **Consumer & marketplaces × Research & reporting** — resolved by the C9 split below. Calnita (domain: consumer) carries `research`, backed by a real CV fact ("pioneered user research initiatives... shaped the roadmap").

**New, opened by the same edit that resolved #1 above:** Ayomide also removed `build-from-zero` from C1 — the command structure, escalation ladder and log fields were set by the Situation Room Lead before supervisors took over, so keeping the tag would claim design work that belongs to someone else. Civic has only this one case, so **Civic & elections × Build from zero is now unsupported** by the data, even though WRD §4.1 shows it filled. This is a deliberate, informed removal, not an oversight — logging it the same way #3 above was logged, as a case where the WRD's checkmark doesn't hold up against what the work actually was. If C1's still-open `built[4]` item (what she personally set up for her own desks beyond the briefing) comes back with a real structural contribution, that could reopen the tag; until then it stays off, per her own instruction.

## Stakeholder capability — open design question, not yet decided

Ayomide flagged that `stakeholder` appears in 6 of 8 distinct capability-sets in the data — a tag that common to nearly everything proves nothing on its own. Two options on the table, neither applied yet:

- **Split it** into two more discriminating capabilities — vendor/contract management vs. partner/sponsor development. Her own original example was the Flutterwave sponsorship pipeline as partner/sponsor development — that case has since been removed entirely (see the Lendsqr swap below), so the clearest example of that flavour is currently gone from the data; a venue contract (plausibly C5, Osun Tech Festival) is still the vendor/contract-management example. This is a change to the `CAPABILITIES` axis itself (6 columns → 7), which means redoing the column-order and segmentation analysis from Step-3-prep — a real amount of rework, and accurate per-case reclassification of the other stakeholder-tagged cases (C1, C2, C4, C7, Lendsqr) can't be done confidently yet since most of their six-block narratives are still `[NEEDS INPUT]`.
- **Leave it as one column, but stop counting it as evidence** — treat it as context rather than a claim (e.g. don't let it count toward the "how many domains does this capability appear in" argument the matrix is built to make).

Recommendation: defer the actual choice until Phase 0 content exists for the stakeholder-tagged cases — right now a split would be executed mostly by guessing which flavour each case is, which is exactly the kind of invented-claim risk R1 exists to prevent. Flagging, not deciding. **Held, per Ayomide.**

**If this split happens, it's a 6→7 column change to `CAPABILITIES`, and the entire column-order and lane/segmentation analysis (Step 3 prep) reruns from scratch — the decision point is before the P1 cases go into content-writing, not after.** Worth deciding this before Phase 0 gets further along for the P1 cases specifically (C1, C2, C4, C7 — the cases that would need vendor-vs-partner reclassification).

## Review — Step 3

**What was built:** `index.html`, `css/home.css`, `js/matrix.js` (pure lane/segment/label logic, no DOM — testable independent of markup), `js/render-home.js` (builds both views from `data/cases.js` at runtime). Mobile-first: the card list + dual domain/capability filter chips is real static-reachable content; the desktop Coverage Matrix is a progressive enhancement built by JS on top of it. A browser with JS disabled gets the linear card list at any viewport width (verified — see below), satisfying N6.

**Design decisions made while executing (flagging since you said stop taking direction from me on structure, but these are real calls worth your eyes):**
- Mobile default grouping is by **capability**, not domain — per the mobile-grouping decision from the last review round. Clicking a capability chip switches grouping to domain (so a capability-filtered view answers "which industries"). This means cases with 2–3 capabilities appear multiple times in the default view (verified: 24 card instances for 11 cases, matches the real per-case capability counts exactly).
- Credentials strip placed directly below the matrix/card list, not above it — read WRD F13 ("visible without scrolling past the matrix") as "immediately reachable after the matrix," not "before it," since putting it above would have undone the thesis-height fix from the last round.
- Case links point to `work/{slug}/` — these 404 today. Case pages are Step 5's job; this is expected, not a bug.
- The CV download link points to `assets/cv/ayomide-amusan-cv.pdf`, which doesn't exist yet — per your instruction to keep the PDF out of the repo until Phase 1. Also expected to 404.

**The label/tick fix, verified:** Lodgr's bar now shows a ✓ tick in both non-label segments instead of one anonymous blank block — screenshot below. Applied generally (widest segment gets the label, ties go leftmost, every other segment gets a tick), so it holds for any future 3+ segment case without touching the axis.

**Verified for real, not claimed:**
- Ran the actual site in headless Chromium at 1366×768 (desktop) and 375×812/412×823 (mobile), with JS on and with JS off.
- Desktop: matrix builds with all 11 cases across 10 lanes, 0 console errors.
- Mobile: card list renders, 13 filter chips (7 domain + 6 capability), clicking a capability chip correctly re-filters and re-groups (spot-checked "Delivery & release" → 6 cases across 4 domains, matches the data exactly).
- No-JS: `#app-root` stays empty, the `<noscript>` block renders all 11 cases as a plain list — the fallback works.
- Keyboard: Tab reaches the first matrix bar directly after the nav (the hidden mobile duplicate is correctly skipped since `display:none` removes it from the tab order); Arrow Down/Up move between lanes, Arrow Right/Left move within a lane; hover and focus both trigger the same row-dimming, and dimming never touches anything outside `.matrix`.
- Lighthouse (real runs against the local server, mobile config, 5 consecutive runs): Accessibility 100, Best Practices 100, SEO 100 every time. Performance 99 in 4 of 5 runs, 88 in 1 of 5 — see below.

**Real bugs found and fixed during verification, not glossed over:**
1. A CSS specificity bug hid the entire matrix on desktop despite JS building it correctly — two rules targeting the same selector, the wrong one won by source order. Fixed.
2. Cumulative Layout Shift started at 0.446 (a real, visible reflow) because `font-display: swap` (specified by N8) means the fallback font always renders first, and bold Arial/Georgia/Menlo don't match Archivo/Source Serif 4/JetBrains Mono's widths — measured in a real browser, not guessed. Fixed with metric-matched fallback `@font-face` declarations (`size-adjust`, `ascent-override`, `descent-override`) calibrated against real rendered text, which brought Performance to 99 and CLS to ~0 in most runs.
3. **Not fully fixed:** in 1 of 5 runs, a small residual shift (CLS 0.226, Performance 88) still shows up from Source Serif 4's swap. Root cause is diagnosed — the mobile view's default capability-grouping renders 24 card instances, and any sub-pixel-per-line rounding difference between the fallback and real font accumulates over that much repeated text into a measurable shift far down the page. This is real remaining work, not resolved — flagging rather than claiming it's done. Likely full fix if you want it pursued: reduce how much duplicate content renders by default, which is a design change, not a CSS tweak.

**Not verified:** real Safari/Firefox/Edge cross-browser check (only tested in Chromium); a real screen reader (checked ARIA labels and DOM structure, not an actual VoiceOver/NVDA pass); the print stylesheet (not built yet, that's F17, a Could for v1.1).

## Review — Step 1

**What changed:** created a new, separate repo at `~/Documents/ayomide-portfolio` (kept out of the existing Frobits repo so its security-critical CLAUDE.md wasn't touched). Added this project's own `CLAUDE.md`, a cleaned copy of the WRD, and `data/cases.js` with `DOMAINS`, `CAPABILITIES`, and all 10 `CASES` objects.

**Why `data/cases.js` looks mostly like placeholders:** the WRD's case table (§6.1) gives one sentence per case — enough to fill `slug`, `title`, `org`, `domain`, and `capabilities` — but not enough to write the six-block case narrative (situation, what was missing, what was built, decisions, outcome, artifacts) without inventing detail. Rule R1 says invented detail isn't allowed, so every narrative field is a `[NEEDS INPUT: …]` placeholder naming exactly what's needed. This matches WRD Phase 0 ("Content" — Ayomide writes the six blocks for the six P0 cases).

**Not yet verified:** nothing runs yet — this step is data only, no HTML/CSS/JS pages exist. There's nothing to load in a browser at this point.

## Review — Step 4 and Step 5

**Step 4 — `/work`:** `work/index.html` + `js/render-work.js`. Two equally-weighted filter rows (Domain, Capability), single-select each, combinable (F4). Filter state reads from and writes to the URL (`?domain=...&capability=...`) via `history.pushState`, with a `popstate` listener so back/forward restores the exact prior view (F5) — verified: a direct load of `/work/?domain=consumer&capability=delivery` returns exactly the 2 matching cases (Boldtron, Calnita) with no JS interaction needed first. Active filters also show as removable pills with a "Clear all," per F4's literal wording, in addition to the toggle chips themselves. Empty state (F6) names real adjacent matches — e.g. filtering Fintech + Live ops (no case has both) says what Live ops actually covers and what Fintech actually covers, not a bare "no results." `<noscript>` fallback lists all 11 cases flatly, verified with JS disabled.

**Step 5 — case pages:** `scripts/build-case-pages.mjs`, run with `node scripts/build-case-pages.mjs`. Builds `work/{slug}/index.html` from `data/cases.js` for every case, with a real six-block template (situation, missing, built, decisions, outcome + RAG badges, metrics, artifacts) plus prev/next navigation weighted to a different domain (F11).

**The hard guard, verified by actually running it, not just described:** the script recursively scans every field of every case for `[NEEDS INPUT: ...]`. A P0 case with any open item is refused outright — no file gets written for it — and the script prints exactly which fields are still open, then exits non-zero. Run today: **all 6 P0 cases were refused** (situation-room, roundtable, lodgr, volunteer-network, osun-tech-festival, website-redesign — none has real six-block content yet), and the 5 P1 cases generated as visibly-flagged drafts. This is the correct, expected result right now, not a bug — it's proof the guard actually blocks what it's supposed to block. **The script's own stdout, not this file, is now the authoritative list of what's still needed per case** — re-run it any time for the current, real state rather than trusting a hand-maintained list here that can drift.

**NEEDS-INPUT flagging:** any placeholder text that does make it into a generated page (P1 drafts only — P0 pages with placeholders never generate) renders through `.needs-input` — bright yellow background, dashed red border, a `⚑ NEEDS INPUT —` prefix — and the whole page carries a dashed-red draft banner at the top. Not stylable-as-real-copy by accident.

**C1 and C3 content:** structural placeholders at realistic block proportions (situation ~3, missing ~2, built 4–6, decisions 2–3, outcome 2–3, artifacts 2–3), per instruction. C1 uses real facts from Ayomide's own discovery-Q&A answers (title, dates, entity, team size ballpark, channel types, what broke) where she gave them, and an open, specific question everywhere she didn't. C3 has no discovery pass done yet, so it's entirely open questions — that's expected, not a shortfall in effort.

**Schema note:** `situation[]` and `missing[]` are now arrays of one-entry-per-sentence (matching how `built[]`/`decisions[]`/`outcome[]` already work) so a real sentence and an open question can sit in the same block without contaminating each other. Only applied to C1 and C3 so far — the other 9 cases still use the old single-string-per-array shape from Step 1, pending their own discovery pass.

**Not yet verified:** Lighthouse wasn't re-run after these two steps — new pages (`/work`, 5 case pages) haven't been scored yet. Cross-browser and screen-reader passes still outstanding from Step 3, unchanged.

## Review — C1 real content (situation-room-osun-election)

**What changed:** replaced C1's structural placeholders with Ayomide's real written account. Capabilities changed from `['live-ops', 'build-from-zero', 'stakeholder']` to `['stakeholder', 'live-ops', 'research']` — see the mismatch log above for what that opens and closes. `entityLine` updated to her exact wording. `situation`, `missing`, and `built` are now substantially real; `decisions`, `outcome`, `metrics`, and `artifacts` still carry genuine open questions — hers, not invented.

**A real bug in the guard, found and fixed before it mattered:** the first draft of `decisions[]` embedded a `[NEEDS INPUT: ...]` marker mid-sentence, in the same string as real setup text. `isNeedsInput()` only checked whether a string *started with* the marker, so those three embedded questions were invisible to both the publish guard and the page renderer — they'd have rendered as plain unstyled text, and the guard would have let the page publish with three real open questions silently sitting in it. Caught by actually re-running the build script and noticing the open-item count didn't match what should have been there (14 shown vs. 17 actually present). Fixed two ways: split every decision into a separate real-sentence entry and open-question entry (matching how `situation[]`/`missing[]`/`built[]` already work, and matching the rule stated when this content was requested — a placeholder is never prose with a note attached), and broadened `isNeedsInput()` to check anywhere in the string, not just the start, as a second line of defence.

**Verified:** re-ran `scripts/build-case-pages.mjs` — C1 is still correctly refused (17 real open items, precisely listed), which is the right outcome given what's actually still unknown. Checked the home page matrix directly: Civic's bar now shows a small `STK` tick over Stakeholder/vendor (a gap over Delivery), with the "Accord Party / 2026" label sitting over the wider Live ops + Research span — the widest-segment-gets-the-label rule applied itself automatically to the new shape with zero code changes, which is what having a real single source of truth is supposed to buy.

**Content-safety check:** re-read the new text against Ayomide's stated constraints — no colleague names anywhere (roles only: Lead, supervisors, desk officers), no reproduction of the Lead's briefing, nothing that maps the agent network by location or reproduces contact information. R3's operational framing holds throughout — no political position, no claim about the election's outcome.

## Review — coverage check + C1's three incidents

**Coverage check, now automatic:** `scripts/build-case-pages.mjs` prints per-capability domain coverage on every run, flagging any domain whose support for a capability rests on exactly one case. Confirmed by running it: **Build from zero and Live ops, fixed date are now the most fragile columns** — every domain that has either of them has it through exactly one case (Build from zero: Lodgr for Hospitality, Volunteer Network for Infrastructure; Live ops: Situation Room, Roundtable, and Osun Tech Festival, one each). Delivery and Research are safer — each has at least one domain backed by two cases (Developer/web infrastructure, via the Langovest/HostMeNG pair). Ships it herself is single-case by design (Frobits) and isn't a concern the same way. **Not a decision, a standing check** — this now runs on every build rather than needing someone to think to ask, per instruction.

**Metrics: left alone, per instruction.** C1's three metric slots stay as structure; the rule going forward is fewer real numbers beats three with weak method notes, so when real figures come in, cut back rather than pad out to fill the existing slots.

**C1's three incidents — two became real decisions, one became a fact instead of a decision.** Security reports from party agents were escalated through a verify-then-escalate pattern (supervisors → ward-agent verification → security only once confirmed) — real content now, with a narrower open question left for whether that verification step ever carried a real cost. Election-day unreachability of some polling-unit agents was handled by falling back to ward and LGA-level party chairmen for results — also real now, with an open question about what that fallback actually cost in precision. The third question (two agents contradicting each other) got a direct answer — it rarely happened, because agents were each assigned a single LGA — which isn't a decision with a trade-off, so it doesn't belong in `decisions[]` as one. Moved into `situation[]` as the structural fact it actually is, rather than forcing a trade-off narrative onto something that wasn't really a fork. C1 is now at **16 open items**, down from 17.

## Review — C1 closed out, first case to pass the publish guard

**What changed:** applied the two-path rule to every remaining open item in C1. `built[4]` (a bare discovery question with nothing behind it) and both decision follow-ups on cost were removed as refinements-not-gaps — the decisions themselves are substantially told; what verification cost is a detail, not a missing fork. `outcome[]`, `metrics[]`, and `artifacts[]` are now empty arrays — none had real content, and per instruction an absent block is honest where a block of placeholders is debt. Nothing was invented to fill any of it; every deletion removed a bare question, not a stated fact.

**Template fix that came with it:** the case-page generator now skips the Outcome and Artifacts `<section>`s entirely when both their arrays are empty, instead of rendering an empty heading with nothing under it. This wasn't cosmetic — without it, C1's page would have shown two orphaned section headings for content that doesn't exist yet, which reads as a gap dressed up as a section.

**Verified, not claimed:** re-ran `scripts/build-case-pages.mjs` — C1 now generates as `(complete)`, the first P0 case to pass the guard for real. Loaded the actual generated page in a browser: zero `.needs-input` spans, no draft banner, capability tags correctly show only Stakeholder/vendor, Live ops and Research & reporting. Prev/next nav correctly points to Frobits (Consumer) and the Africa Infrastructure Roundtable (Infrastructure) — different domains on both sides, per F11.

Five P0 cases remain refused (roundtable, lodgr, volunteer-network, osun-tech-festival, website-redesign) — none has had a real content/discovery pass yet. That's expected, not a regression.

## Review — six data changes: Lendsqr swap, renames, 2026 dates

**Fintech swap:** removed `fintech-sponsorship-pipeline` entirely (not merged, not archived — gone) and added `lendsqr-product-operations` in its place. CV-sourced facts only (role, org, dates, the two capability descriptions); the six blocks are open questions since no discovery pass has happened. Fintech now rests on this one case, as instructed.

**Coverage check, re-run and reported:** the swap changed nothing structurally — Fintech supported exactly `{stakeholder, research}` before and supports exactly the same two after, just via Lendsqr instead of the removed case. **No capability column lost a domain.** Full table is identical to the last run except the Fintech row now names `lendsqr-product-operations` instead of the removed slug.

**Renames applied:** "Three beauty MVPs" → "Calnita MVP"; "Clea and Pushbio enhancements" → "Clea and Pushbio Project Management." Slugs left untouched (URLs are stable identifiers; renaming a title shouldn't silently break a link elsewhere).

**Dates:** added year-precision 2026 dates to the Africa Infrastructure Roundtable (2026 → 2027, matching what its own title already stated — WRD-sourced, not new), the Volunteer Network, the website redesign, Boldtron, Lodgr, and Frobits (all 2026, single year). Exact months/days remain unconfirmed and are noted as such in code comments — this closes the "no date shown" gap on every card except Osun Tech Festival, which wasn't in the instruction.

**Verified, not claimed:**
- Swept the full data file for embedded/malformed NEEDS INPUT markers — clean.
- Ran the build script: Lendsqr generates as a correctly-flagged P1 draft (loud placeholder styling, draft banner, zero metrics section since `metrics: []` — the empty-section skip built for C1 generalised correctly to a second case without any template change needed).
- Removed the orphaned `work/fintech-sponsorship-pipeline/` directory left over from before the case was deleted — a stale generated page for a case that no longer exists in the data would have been a real publish risk.
- Loaded the actual home page matrix: 11 bar links (was 11 before too — a straight swap), zero console errors, Fintech's bar renders as `LENDSQR / 2025` with the identical gap shape the old case had (same capability pair, same axis positions). Every one of the six named cards now shows a year on the matrix.
- Loaded Lendsqr's generated page directly: correct title, entity line, capability tags, and prev/next nav correctly shows the renamed "Clea and Pushbio Project Management" title and weights to a different domain (Developer/web infrastructure) per F11.

## Review — C1 confidentiality rewrite + five fixes

**C1** now carries the deliberately reduced version supplied verbatim. This is a final version, not a draft awaiting more detail — no restoring the earlier mentions of media/security/standby-support escalation, no named individuals, no description of how escalation to security worked. Decisions are fully resolved text now (no embedded or trailing open questions); outcome/metrics/artifacts stay empty. Verified by loading the real page and scanning its text — the only "media" match was inside "immediate," confirmed a false positive.

**Label rule refined:** leftmost segment wins unless a wider segment elsewhere is more than one column wider. Fixes Civic (1-wide leftmost vs. 2-wide elsewhere) without touching any other case, since every other multi-segment case currently has equal-width segments.

**Everything else:** Lendsqr's spelling checked field-by-field — correct everywhere, no transposition. Osun Tech Festival now has real dates (19–20 Feb 2026) — last card without one, now every card shows a date. Two card titles no longer carry bare metrics ("Langovest Volunteer Network," "Langovest website redesign") — the numbers were already duplicated into `metrics[]` with method notes, so nothing was lost. `/work` now sorts P0 before P1, then most recent first by dateEnd/dateStart, verified against the actual rendered order including a three-way tie at year-only "2026" resolving to stable data-file order.

## Review — Step 6: /about, /contact, /method, SEO + JSON-LD

**Content sourcing:** every fact on /about traces to what was supplied verbatim (the FMC Abeokuta review-committee role, the chairman's advice, the Coursera pivot, the CV's Project Manager bullets, education, four certifications) or is flagged `[NEEDS INPUT]` where it isn't (Tools — no tools list has been supplied, so the section says so plainly rather than guessing). The Biochemistry-to-product throughline is told as plain narrative, not asserted as a thesis — no sentence claims "this is the same method," the parallel is left for the reader to notice in how the FMC workflow is described (an owner and a standard at each stage).

**/method is honest about its own evidence gap, per instruction.** All five steps are derived directly from C1's real `built[]`/`decisions[]` content — nothing borrowed from a generic PM framework. The page states plainly, in its own copy, that every step is currently evidenced by one case only, and that the site's "repeats across domains" claim can't be fully tested yet. This will need revisiting once more cases have real content — either the steps hold up elsewhere (good) or some turn out to be specific to the Situation Room engagement (also worth knowing, and worth rewriting this page honestly if so).

**SEO + JSON-LD:** every page now has a unique title and meta description. Person schema on home and /about; CreativeWork schema on every case page (name, description, about/domain, keywords, creator, and temporalCoverage when real dates exist). Case-page meta description and JSON-LD both pull from `situation[0]`/`entityLine` at build time via a shared `metaDescriptionFor()` helper that explicitly skips anything `[NEEDS INPUT]`-flagged — this was built specifically so a future confidentiality edit like C1's propagates to metadata automatically, and verified directly: C1's live meta description and JSON-LD description are both the current reduced text, with zero mention of media, security, standby support, or escalation methodology.

**OG images: real, not placeholders.** Generated 16 branded 1200×630 PNGs (5 pages + 11 cases) via a Playwright screenshot of an HTML template using the site's actual fonts and palette — every title is real, pulled from data, nothing invented. Deliberately did not add `playwright` as a project dependency to generate these — that would mean committing a ~180MB Chromium download and a node_modules tree into a project whose whole ethos is zero runtime dependencies, and CLAUDE.md's "no extra libraries without asking" rule. Generated the images using a temporary symlink to an external Playwright install, then removed the symlink — only the resulting static PNGs (652KB total) are committed. `scripts/build-og-images.mjs` documents this trade-off in its own header comment for whoever regenerates these later.

**Real bug fixed in passing:** `index.html`'s and `work/index.html`'s no-JS fallback card lists were badly stale — a dead link to the removed sponsorship-pipeline case, two un-renamed titles, several missing dates. This had drifted silently for several turns because nothing forced it to stay in sync with `data/cases.js`. Wrote `scripts/build-noscript.mjs` to regenerate both from the data directly (grouped-by-domain for home, P0-then-recency for /work, matching `js/render-work.js`'s own ordering exactly), closing off this entire class of bug rather than hand-patching the current drift.

**Also added:** a site-wide "Contact" footer link (home, /work, every case page, and cross-linked between /about, /contact, /method) — /contact wasn't reachable from anywhere before this step.

**Verified:** all 5 new/changed top-level routes return 200 locally; all 16 OG images return 200; every internal link on the three new pages resolves to a real page; C1 still passes the publish guard after the metadata changes; no `[NEEDS INPUT]` text leaks outside its intended flagged span anywhere in the new pages.

**Blocker, not fixed this step, flagged per instruction:** the CV PDF still doesn't exist. Every "Download CV" link across the entire site (header, footer, /contact) points to `assets/cv/ayomide-amusan-cv.pdf`, which 404s. **This has to be resolved before Step 7** — an audit pass against a site where the primary persistent call-to-action is a dead link everywhere isn't a real audit.

## Review — real CV received, cross-checked against every case

**The full CV arrived this turn.** Cross-checked it against everything already in the data, not just the specific items asked for.

**Confirmed already correct, no change needed:** /about's FMC Abeokuta title, dates (Sept 2022 – June 2023) and all three experience bullets matched the CV exactly. /contact's email, LinkedIn URL, and "open to employment and contract work" were already live from an earlier turn — confirmed by reading the file directly rather than assuming.

**Tools added to /about**, sourced verbatim from the CV's "Area of expertise and tools" section — the summary line plus four categorised tool lists. No more `[NEEDS INPUT]` there.

**Surgical, CV-sourced corrections applied across other cases** — precise facts resolving existing open items, not new narrative synthesis:
- Africa Infrastructure Roundtable, Volunteer Network, Website redesign: dates corrected to March–August 2026 (her actual Langovest employment period) — previously inferred from the title alone as 2026→2027, which conflated her employment dates with the 2027 pipeline's *target* date. Flagging this as a real correction, not a refinement: the previous date was a reasonable inference that turned out to be wrong once the real source arrived.
- Lodgr: `org` resolved to "Lodgr" (the CV names it directly, same pattern as Boldtron/Calnita). Lodgr and Boldtron dates corrected to September 2025 – March 2026 (the Independent PM contract's bounding period — the CV doesn't say which weeks within it belonged to each engagement, so this is the honest bound, not a precise range).
- HostMeNG: `dateEnd` resolved to January 2025 (was open). `built[]` gained a real sentence (the CV's actual retention-driving method: iterative feedback integration and market-trend analysis) and a situation[] sentence about the cross-country team. The metrics method note was narrowed rather than closed — the CV names *how she drove* the 15% improvement, not *how it was measured* (cohort, tool, period), and those are genuinely different questions.
- Calnita: `dateStart` corrected to June 2023 (was year-only). Similar treatment: real sentences added to `built[]` and `situation[]`, metrics method narrowed rather than closed for the same reason.
- Lendsqr: all three CV bullets are now real `built[]` content, replacing what was previously a single open question.

**Deliberately not done, flagged for a decision:** the CV contains enough material to substantially advance `situation[]`/`missing[]`/`decisions[]` for several more P1 cases beyond what's listed above — but "decisions and trade-offs" specifically isn't something a CV ever states (a CV describes what happened, not what was given up to get there), so that block still needs real input regardless of how much CV material exists. Didn't attempt a full six-block rewrite for any case beyond the surgical fixes above without being asked — that's a bigger authorship decision than "here's a document, check it," and worth Ayomide's explicit go-ahead rather than assumed scope.

**Verified:** swept the full data file for embedded markers (clean), re-ran the build script (coverage table unaffected — none of today's changes touched capability tags; C1 still `(complete)`, the same five P0 cases still correctly refused), regenerated both no-JS fallbacks and confirmed the new dates render correctly on the live matrix (Lodgr "2025 → 2026", Boldtron "2025 → 2026", HostMeNG "2024 → 2025", the three Langovest cases "2026").

## Review — CLS root cause found (it wasn't the font swap), Motion A/B built, P0 guard no longer 404s

Three asks this turn: fix the still-bistable CLS, add two motion candidates, and stop the publish guard from leaving dead matrix bars.

**CLS — the font-swap diagnosis from the last three sessions was wrong.** Reserving `min-height` on `.card-groups` (this session's first attempt, per instruction) made no measurable difference — 4 of 6 Lighthouse runs still hit the exact same score, `0.2260024301336573`, on the exact same element (`.credentials`), before *and* after. That repetition across three unrelated fix attempts (metric-matching, preloading, height reservation) was the tell that the theory itself was wrong, not the execution. Captured the actual `layout-shift` PerformanceObserver entries (previous/current rects, not just Lighthouse's summary) and found the real cause: `#app-root` ships **empty** in the static HTML, and `js/render-home.js`'s `init()` appends the ~4191px mobile card list into it *after* first paint. `.credentials` and `.site-footer` render at `y≈389` on the first painted frame (everything above them is still just header + hero, since the card list doesn't exist yet), then get shoved down by ~4200px in a later frame once JS populates `#app-root` — a real, deterministic, JS-timing-driven shift with nothing to do with any font. Fixed by reserving `#app-root`'s own settled height (4191px) via `min-height` inside `@media (max-width: 767px)` — scoped to mobile only, since above 768px the matrix (much shorter) is what actually ends up visible, and reserving mobile's height there would leave dead space under it. Result: **6 of 6 Lighthouse runs now score CLS=0, Performance=98** — no longer bistable. Verified desktop is unaffected (`#app-root` sits at its natural 591px matrix height at 1366px, no gap). Also found and fixed a second, smaller, genuinely font-driven shift along the way: `.credentials`' mono labels wrap into rows whose height depended on the active font's default line-height (`normal`), which differed by ~2px/row between real JetBrains Mono and its Menlo fallback even with `size-adjust`/`ascent-override`/`descent-override` calibrated — fixed with an explicit `line-height: 1.25rem` on `.credentials .credential`, confirmed to make both fonts render the block at an identical 133px. Both fixes are needed; neither alone resolved the full picture, since the `#app-root` shift subsumed and outweighed the credentials one in every measurement.

**Motion A (matrix resolves on load, one-shot, first paint only).** Bars and gap connectors fade + draw in (`opacity` + `scaleX`, `transform-origin: left`) with delay staggered by DOM order across a fixed 300ms budget plus a 250ms per-bar duration — structurally capped under 600ms regardless of how many cases exist, since the delay is spread proportionally rather than hardcoded per bar. Skipped entirely under `prefers-reduced-motion` at the JS level (no class added, no timer started) rather than only neutralised in CSS. The triggering class is removed by `setTimeout` once the sequence finishes, specifically so its animation `fill: both` end-state can't outlive it and silently block the existing `.is-dimming` hover/focus opacity rules (a finished CSS animation with `fill:both`/`forwards` otherwise keeps winning the cascade for that property over a later plain rule). Verified: opacity is `0` immediately after load, `1` after 700ms, `matrix-resolving` class is gone by then, zero `layout-shift` entries recorded during the whole sequence, and reduced-motion mode shows bars at full opacity with the class never added at all. Screenshotted mid-sequence and settled.

**Motion B (live case count beside a hovered row/column label).** Added a `.hover-count` span (real text, e.g. "· 3", present from the initial build — never inserted later) to both `.domain-label` and `.col-head`, held at `opacity: 0` until the element gets `.is-active` (already the mechanism the existing hover/focus dimming system uses), so revealing it is a pure opacity change that can never move the label or the header row. Building this exposed a real pre-existing bug in `setActiveColumn`: it indexed `.col-head` elements without accounting for the corner cell always being first in DOM order, so hovering a column header never actually applied `.is-active` to that header at all (bars in the column still highlighted correctly, since that logic doesn't share the same index). Fixed the off-by-one (`colIndex + 1`, not `colIndex + 0`) — this was silently broken before Motion B needed it. Verified: opacity goes `0→1` on hover for both a domain-label and a col-head, with the label's own bounding-box width identical before and after (200px, unchanged). Screenshotted baseline, row-hover, and column-hover states.

**Guard behaviour for incomplete P0 cases.** `scripts/build-case-pages.mjs`'s `run()` no longer skips writing a page for a P0 case with open `[NEEDS INPUT]` items — it now generates one with the identical draft-banner/`.needs-input` treatment P1s already got, and separately tracks those cases for the report. The exit-code-1 deploy gate is unchanged: the build still fails and still prints every P0 case's exact open items, it just no longer leaves a matrix bar pointing at nothing while it does. Ran the build: all 11 cases now generate (`work/{slug}/index.html` exists for every one, including the 5 P0s that were previously refused), C1 is unaffected (still the lone `(complete)` page), exit code is still `1`, and the report still names the same 5 P0 cases with the same open items as before. Verified live against the actual home page: all 11 `.bar-seg[href]` links on the matrix now return `200`, not one `404`.

## Review — Step 7 audit re-run against the motion/guard changes

The prior pass was stale — motion touched `render-home.js`'s matrix build, and the guard change touched every case page's generation path. Re-ran keyboard-only, JS-disabled, and 375px against all of it.

**Keyboard-only, full site.** Tabbed through home, `/work`, three case-page variants (the one complete case, a newly-generated P0 draft, and an existing P1 draft), `/about`, `/contact`, `/method`. Skip-link is the first stop everywhere, focus is visible on every stop, no real traps. `/method`'s tab sequence looked like a trap at first (five consecutive stops with identical text/href, since every method step currently evidences the same one case — its own stated caveat), but confirmed via a per-node identity check that focus advances through 12 genuinely distinct DOM nodes; not a bug, just repetitive content. Matrix arrow-key nav (Down/Right) still moves between real cases correctly, and the focus ring still renders.

**Found a real bug, fixed it: keyboard focus could land invisible mid-resolve.** Tabbing immediately after page load could reach a matrix bar before Motion A's staggered delay had elapsed for it — at that moment the bar (and its focus-ring box-shadow, drawn on the same element) was at `opacity: 0`, so a fast keyboard user got a focused-but-invisible link with no visible indicator at all. Fixed by having any `focusin` inside the matrix immediately end the resolve sequence for every bar, not just the one focused — same effect as if the animation had already finished naturally. Verified directly: focused element is `opacity: 1` the instant it receives focus regardless of where in the sequence Tab arrives, zero elements remain at `opacity: 0` afterward (the ones still at `0.28` are the pre-existing, correct hover/focus dimming feature, not a leftover bug), and the animation is still one-shot — nothing re-triggers it. Re-ran a 3-pass Lighthouse spot-check after this fix: CLS still `0` on all three, confirming it didn't reopen Task 1's fix.

**JS-disabled.** `#app-root` stays empty (nothing depends on it), the `<noscript>` fallback renders as real, clickable markup on home (12 links found) and `/work` (11 case links), and case pages — including the newly-generated P0 drafts — render fully with zero JavaScript, since they're static HTML from build time. Specifically confirmed: the P0 draft page's `.draft-banner` and all 7 of its `.needs-input` spans are present and styled correctly with JS off.

**375px.** Zero horizontal overflow on every page checked (home, `/work`, both case variants, `/about`, `/contact`, `/method`); home correctly shows the mobile card list with the matrix hidden below the 768px breakpoint. Screenshotted home and the P0 draft page at 375px: the domain/capability chip rows scroll horizontally within their own strip (by design, not a page-level overflow), and the draft banner + NEEDS INPUT flags render loud, legible, and correctly styled on mobile — amber background, dashed red border, flag icon, no layout breakage.

**Net result:** one real, fixed bug (the mid-resolve focus issue); everything else the audit checked was already correct. Step 7 can now be considered current against this turn's changes.

## Review — first person sweep, CV-filled case content, new Federal Medical Centre case

Three asks: switch the whole site's voice from third to first person, fill in every case's open blocks from the updated CV under a hard limit (no invented decisions/trade-offs, no invented metric methods), and add an 11th CV role as a 12th matrix case.

**First person sweep.** Grepped every `.html`/`.js` file for `she|her|hers|herself` and converted every rendered-content hit: the home thesis line and its meta/OG description, `/about`'s two "Before product" sentences, the `CAPABILITIES` label `'Ships it herself'` → `'Ships it myself'` (propagates to every column header, card tag, and JSON-LD keyword through the single-source-of-truth data file), and C1's one first-person sentence. Also converted `/method`'s one impersonal "The site's own claim is..." to "My claim is..." for voice consistency, even though it used no gendered pronoun — the rest of that page was already pronoun-neutral prose, not third-person about Ayomide, so there was nothing else to convert there. Scoped out, deliberately: code comments in `data/cases.js` and `js/render-home.js` (developer documentation, not site content) and `CLAUDE.md`/`WRD-ayomide-amusan-portfolio.md` (project requirements docs, not part of the deployed site) — left in third person on purpose, not missed. Also deliberately left the internal `ships-it-herself` capability *slug* unchanged — it's a kebab-case id used in URLs/filters/data plumbing, never rendered directly (confirmed via `CAPABILITY_TICK`, which already maps it to the gender-neutral `SHIP`); renaming it would touch every case's `capabilities[]` array and the filter-URL scheme for zero visible benefit. Verified with a final repo-wide grep: zero `she/her/hers/herself` remain in any rendered `.html` output anywhere on the site. There is no `alt=` text anywhere on the site to convert (no `<img>` tags exist — OG images are referenced only via `<meta>` tags), contrary to what the instruction implied might need checking.

**CV-filled case content, under the hard limit.** Went case by case against the actual CV bullets. Filled `situation[]`/`missing[]`/`built[]` wherever a bullet directly stated or directly entailed the fact (e.g. "established... from zero" → real `missing[]`; "scoped and built the pipeline" → the pipeline didn't exist before, stated as such). Left `missing[]` open wherever the CV only described an action, not an explicit before-state, rather than inferring a gap that wasn't stated (HostMeNG, Calnita, Lendsqr, FMC all still have open `missing[]` for this reason). `decisions[]` was left open on every single case, including Boldtron, where the CV's "setting roadmap and dependency decisions" is a process description, not a stated trade-off — flagged explicitly in a code comment so a future pass doesn't mistake that phrase for license to fill it in. `metrics[]` stayed exactly as narrow as the CV supports: Lodgr's and Boldtron's invented-number placeholders were replaced with a genuinely empty array (no CV number exists for either), and every real number kept its `method` field open where the CV states the *driving activity* but not the counting mechanics (volunteer network, website redesign, HostMeNG retention, Calnita on-time rate all still have open `method`). Filled five `outcome[]` entries with `status: 'green'` — volunteer network, website redesign, HostMeNG, Calnita, Osun Tech Festival — every one restates a result the CV itself frames as achieved (a percentage, a headline count, or "managed... to delivery" against named dates), never a status inferred beyond what's stated; flagging this classification choice explicitly since RAG status wasn't named in the hard limit but is still a judgment call, not raw fact. Frobits got no updates — it's Ayomide's own product, not a CV bullet, so there was nothing to fill.

**One CV fact with no home:** the Langovest bullet's "converting 25% of contacts to positive response" belongs to the sponsorship/partnership-outreach work, which has no case in the matrix — that case (`fintech-sponsorship-pipeline`) was removed entirely in an earlier session per explicit instruction, not merged into anything else. Not reviving it without being asked; flagging that this specific stat currently has nowhere to live.

**New case: Federal Medical Centre.** Added `fmc-clinical-research` (P1, `stakeholder` + `research`, Sept 2022 – June 2023) in a new `health-research` domain, placed last in `DOMAINS`. `situation[]`/`built[]` filled directly from the CV's three bullets (investigator recruitment/compliance documentation, literature review, data collection tracking); `missing[]`/`decisions[]`/`outcome[]` stay open — the CV describes ongoing responsibilities here, not a before-state or a stated result. Generated its OG image via the same temporary-Playwright-symlink workaround as before (symlink added, `build-og-images.mjs` run, symlink removed immediately after — confirmed `git status` shows no `node_modules` leak). Re-ran the coverage check: **Stakeholder / vendor and Research & reporting both grew from 5 to 6 domains**, each gaining Health & research as a new single-case dependency (flagged as such in the report, same as every other single-case domain). No other capability's coverage changed — Health & research doesn't carry `build-from-zero`, `delivery`, `live-ops`, or `ships-it-herself`.

**Found and fixed a real regression while doing this:** adding a 12th case grew the mobile card list from 4191px to 4567px settled height at 412×823, which made Task 1's CLS-fix `min-height` on `#app-root` (still at the old 4191px value) too small for the new content — exactly the failure mode that fix exists to prevent, since a reserved floor only works if real content doesn't grow past it. Re-measured and updated the value to 4567px, with the comment now spelling out that this number is tied to case count/content and must be re-measured on every future case addition, not treated as a one-time constant. Re-verified: 3/3 fresh Lighthouse runs still CLS=0 after the fix.

**Report — open items by case** (counting `situation`/`missing`/`built`/`decisions`/`outcome`/`metrics`; total in parentheses also includes `artifacts` and `org`, matching what the publish guard itself blocks on):

| Case | Priority | Open (6-block) | Blocks complete | Total incl. artifacts/org |
|---|---|---|---|---|
| situation-room-osun-election | P0 | 0 | 6/6 | 0 |
| africa-infrastructure-roundtable | P0 | 3 | 4/6 | 4 |
| lodgr-booking-platform | P0 | 9 | 2/6 | 11 |
| langovest-volunteer-network | P0 | 2 | 4/6 | 3 |
| osun-tech-festival | P0 | 2 | 4/6 | 4 |
| langovest-website-redesign | P0 | 2 | 4/6 | 3 |
| boldtron-marketplace-app | P1 | 3 | 4/6 | 4 |
| lendsqr-product-operations | P1 | 5 | 2/6 | 6 |
| hostmeng-clea-pushbio | P1 | 4 | 2/6 | 5 |
| calnita-beauty-mvps | P1 | 5 | 1/6 | 6 |
| frobits-ai-music-whatsapp | P1 | 6 | 1/6 | 7 |
| fmc-clinical-research | P1 | 4 | 3/6 | 5 |
| **Total** | | | | **58** |

**Which cases now have five of six blocks complete: none.** The honest ceiling under this turn's hard limit is 4/6 for every case except C1 — `decisions[]` is open on all eleven remaining cases with no exception, and reaching 5/6 would require *every other* block resolved too. Five cases (AIR, volunteer network, Osun Tech Festival, website redesign, Boldtron) hit exactly that 4/6 ceiling: real `situation`/`missing`/`built` plus either a real `outcome` or a legitimately-empty `metrics`, blocked from 5/6 only by `decisions` and one other block the CV genuinely doesn't answer for that case (usually `outcome`, since the CV states actions more often than results). The other cases (Lodgr, Lendsqr, HostMeNG, Calnita, Frobits, FMC) sit lower because the CV supports less for them specifically — Frobits lowest of all (1/6) since it's not CV-sourced content at all.

**Verified:** `data/cases.js` parses cleanly (12 cases, 8 domains, capability labels correct); rebuilt all 12 case pages and both no-JS fallbacks; spot-checked rendered output for the new FMC case and a green-outcome case (screenshots — first-person prose, correct RAG badge, correct prev/next nav wrapping to include the new case); confirmed the coverage-report diff; confirmed zero `she/her/hers/herself` remain in any rendered HTML anywhere on the site.

## Review — Step 8: Coverage Matrix retired, landing page rebuilt

Went through a visual design pass first (a Claude Design canvas mockup, iterated across several rounds: structure, then a pastel palette, then copy), landed on a direction, then built it for real. This turn was the "build it" step.

**The Coverage Matrix is gone.** Deleted `js/matrix.js` and `js/render-home.js` entirely rather than leaving dead code, per instruction. `matrix.js` wasn't purely matrix code, though — three build scripts (`build-case-pages.mjs`, `build-noscript.mjs`, `build-og-images.mjs`) imported its `isNeedsInput`/`displayLabel`/`displayDateRange` helpers, and **`js/render-work.js` did too** (`/work`'s own live JS, not dead code at all). Extracted those three functions into a new `js/case-utils.js`, dropped the genuinely matrix-only exports (`computeSegments`, `assignLanes`, `buildMatrixModel`, `CAPABILITY_TICK`), and repointed all four imports. Missing that `render-work.js` dependency would have taken `/work` down the moment `matrix.js` was deleted — caught it by grepping for every import of the file before deleting it, not after.

**The new home page is fully static HTML — no runtime JS at all.** This is the real fix for the CLS bistability this project chased across four separate sessions (font metrics, preloading, `min-height` reservations, and finally the actual JS-timing cause): the root cause was always that `#app-root` shipped empty and got a large card list appended into it after first paint. A static page can't have that failure mode, so rather than reserve space for a JS-driven append, there's no append. The three featured case cards (Situation Room, Africa Infrastructure Roundtable, Osun Tech Festival — the third swapped in for Lodgr per instruction) are generated at *build* time by a new `scripts/build-home.mjs`, the same pattern `build-case-pages.mjs` and `build-noscript.mjs` already use, writing real title/org/domain/date pulled from `data/cases.js` between markers in `index.html` — never hand-typed, never JS-rendered at request time.

**Pastel palette**, added to `css/home.css` as an explicit *extension* of the locked tokens in `tokens.css`, not a replacement — paper/ink/line and the RAG colors are untouched. Two hue families (a soft blue and a soft lavender), each a light "fill" for backgrounds and a deeper "readable" shade for links/labels, since a pastel is too light to pass contrast as text color itself. Verified numerically, not eyeballed: both deep shades clear 5.8:1+ against paper and white (AA, most of it AAA), both fills give ink-on-fill contrast above 12:1, and both hues sit 90°+ from every RAG hue on the color wheel — chosen deliberately so nothing here could be mistaken for a status color, since RAG is explicitly reserved for case outcomes only.

**Portrait placeholder, not a fake photo.** No real photo exists yet, so the hero's image slot is an inline SVG placeholder (dashed border, pastel-blue-tint background, a simple stroke-based person icon, "Add portrait photo — assets/portrait.jpg") with an HTML comment giving the exact three-step swap-in instructions once a real photo exists — save it as `assets/portrait.jpg`, replace the placeholder div with an `<img>` carrying explicit `width`/`height` (so the real image can't introduce a new CLS regression the moment it's added), delete the comment. Caught and fixed one real ordering bug while checking this on mobile: the hero's `flex-direction: column-reverse` put the (empty) placeholder box above the thesis line — the single most important sentence on the page was getting pushed below a box that says "add a photo." Fixed to `column` so the thesis leads on every width; the desktop side-by-side layout (set via its own media query) was unaffected.

**Full Lighthouse, all four categories, mobile config:**
- First pass: Performance 100, Best Practices 100, CLS 0 (3/3 runs) — but Accessibility 92 and SEO 90, both under the ≥95 bar.
- Real, fixable causes: (1) `.credentials-label` had `opacity: 0.8` on top of the already-verified `--lavender-deep`, which silently weakened its contrast below AA — removed. (2) No heading was ever an `<h2>` — "About"/"Selected work"/"Credentials" were all `<p>` tags, so the outline jumped `h1 → h3` (the lead work-card title) then landed an `<h2>` afterward (the close heading), completely out of order. Converted the three section labels to real `<h2>`s and made all three work-card titles `<h3>` (they'd been an inconsistent `h3`/`h4`/`h4` mix) — fixed in `build-home.mjs`'s generator, not just the generated output, so a rebuild doesn't reintroduce it. (3) "Read more" is meaningless out of context to a screen-reader user navigating by links list — changed to "Read more about me".
- Second pass after fixes: **Performance 100, Accessibility 100, Best Practices 100, SEO 100, CLS 0 — all three runs, all four categories.**
- Spot-checked the other pages that share `home.css`/`case-utils.js` (`/work`, `/about`, `/contact`, `/method`, one case page): all still 97–100 across every category, all CLS ≤0.065. Two of those (`/work` 0.065, `/contact` 0.045) carry a small pre-existing CLS that predates this session and wasn't touched by anything here — still well under the 0.1 "good" threshold and not blocking the ≥95 requirement, but flagging it since it's real and unexplained.

**Keyboard, JS-disabled, 375px:** clean tab order (skip-link → nav → About link → three work cards in reading order → close CTAs → footer), every stop focus-visible, no traps. Confirmed the entire page — thesis, all three work cards, the portrait placeholder — renders correctly with JavaScript fully disabled, which is now trivially true rather than a fallback to maintain, since there's no JS-only content left on this page at all. Zero horizontal overflow at 375px on every page checked.

**Found and left alone:** `index.html` showed as deleted in `git status` at the start of this turn, with nothing else in the working tree touched and no commit history showing it. Nobody involved in this session deleted it. Since the whole point of this turn was to write a brand new `index.html` anyway, there was nothing to restore — but flagging it here rather than silently treating it as expected, since an unexplained file deletion outside anything this session did is worth Ayomide knowing about even after the fact.

**Not done, unchanged from every prior flag:** the CV PDF still doesn't exist — every "Download CV" link across the whole site, including the new hero, still points at a 404. Still the same live-site defect it's been since Step 6, still needs resolving before this domain is shared with anyone. Also still open: `assets/portrait.jpg` itself (the placeholder is honest about this, not a gap I introduced), and job 2 from the same brief (the Railway deploy prep) hasn't been started.

## Review — Step 9: one visual system, not two

`/work`, case pages, `/about`, `/contact`, `/method` were still the old matrix-era language (grey mono, hairline borders, no colour) while the landing page had moved on to the pastel system — instructed to bring all five onto the landing page's system: same palette, same spacing rhythm, same component treatment, same interaction quality. Typefaces stayed exactly as they are, per instruction — this was a layout-language problem, not a type problem.

**What changed, by page:**
- **Case pages** (`css/case.css`, and `scripts/build-case-pages.mjs`'s template): domain eyebrow and every one of the six section headings (`Situation`, `What was missing`, ...) now carry a short colour-deep label with a pastel-blue underline rule, replacing the grey mono + full-width hairline. Capability tags in the header are now pastel-blue-tint fills, not grey-bordered pills. Artifact list items are pastel-lavender-tint blocks, not hairline boxes. Metric values are coloured. Case nav (prev/next) colours on hover instead of just darkening. **RAG badges and `.needs-input`/`.draft-banner` were not touched at all** — confirmed via diff that no rule inside either block changed; they're still exactly as loud as before, more so now that the surrounding page is calmer pastel rather than stark monochrome (screenshotted a draft P0 case to confirm — the amber/red flags still pop immediately).
- **`/work`** (`css/home.css`'s shared filter/card-list rules — same rules `js/render-work.js` builds against at runtime, confirmed nothing there is dead code before touching it): filter chips' active/pressed state is now pastel-blue (domain row) or pastel-lavender (capability row) instead of one flat black fill for both; case cards alternate pastel-blue-tint/lavender-tint backgrounds instead of uniform hairline-bordered boxes, with the org line coloured to match.
- **`/about`, `/contact`, `/method`** (`css/page.css`): page eyebrow gets the same coloured-rule treatment as case pages. Experience entry, credential list items, method steps, and contact links all moved from hairline-top-border rows to pastel-tint blocks, alternating blue/lavender across repeated items (credentials, method steps, contact links) for rhythm — same device used for `/work`'s cards and `/about`'s own credential list.

**A real CLS regression, found and root-caused, not just patched.** First full-site Lighthouse pass after the above: `/about` 93, `/method` 94, the Situation Room case page 91 (well under the ≥95 requirement), with CLS as high as 0.168 on the case page — a real regression, not noise. Captured the raw `layout-shift` PerformanceObserver entries (not just Lighthouse's summary) to find the actual cause rather than guessing, and it took several iterations to fully surface:
1. First guess, partially right: the new `border-bottom`-as-underline-rule technique on inline-block mono headings (`.case-domain`, `.case-block h2`, `.page-eyebrow`, `/work`'s `.card-group h2`) makes their box height depend on the active font's line-height, which still differs by a couple of px between JetBrains Mono and its fallback even with metrics calibrated — the *exact same bug class* `.credentials .credential` in `home.css` was fixed for in an earlier session, just a fresh instance of it in new rules. Added explicit `line-height` to all four. Reduced but didn't eliminate the shift.
2. Same bug, different elements: `.site-header .wordmark` (Archivo) and `.site-header nav a` (JetBrains Mono) had never had an explicit line-height either — pre-existing on every page since the header was first built, just never triggered a large enough score to notice before. Fixed both.
3. Different bug, same symptom: `.page-dek`, `.entity-line`/`.byline` (italic Source Serif 4) and `.case-header .card-tags li` also lacked explicit line-height. Fixed all three.
4. The real root cause, once the above stopped moving the number enough: **only `index.html` had `<link rel="preload">` for the site's fonts — `/about`, `/contact`, `/method`, and all 12 case pages had zero font preloads.** Fonts on those pages were only discovered via the CSS `@font-face` `url()` reference, loading much later than on the home page, which gave the fallback far more time to render (and, on `/about` specifically, real Source Serif 4 vs. its fallback still wraps body paragraphs onto a different number of lines at that width even with `size-adjust` calibrated — a width gap, not a line-height one, and the reflow from that is what a missing preload gives enough time to actually surface). Added the same five preloads `index.html` already had to `/about`, `/contact`, `/method`, and the case-page template (regenerating all 12 pages). This wasn't a bug in anything from today — it was scoped to the home page during an earlier CLS-chasing session and never propagated, sitting latent everywhere else until a heavier styling pass gave it room to show up.
- **Result:** re-ran Lighthouse twice each across 8 representative pages (home, `/work`, `/about`, `/contact`, `/method`, three case pages) after all of the above: **CLS 0.0000 on all 16 runs, Accessibility/Best Practices/SEO 100 on all 16, Performance 98–100 on all 16.** Also re-captured raw layout-shift entries directly (not just the Lighthouse summary) on the three previously-worst pages: empty array, zero shifts, on all three.

**Also verified:** keyboard tab-through on a case page and `/about` — skip-link first, full nav, no traps, every stop focus-visible. JS-disabled + 375px across all 7 page types checked: zero horizontal overflow anywhere, every page still has a real `<h1>` and full body text with JavaScript off. Page weight (home, a case page, `/about`): 134–148KB each, comfortably under the 500KB budget.

## Review — Job 2: Railway deploy prep (not connected, not deployed)

Per instruction: prepared the configuration only. Nothing was connected to Railway and no deploy was triggered — that stays Ayomide's to do from the dashboard.

**The blocker, solved first.** `scripts/build-case-pages.mjs` used to exit 1 whenever a P0 case had open items — correct for a human running it locally (the guard against shipping placeholder content by accident), fatal for a deploy pipeline, which has no way to respond to that prompt. Added `--allow-drafts`: same pages, same draft banners, same NEEDS-INPUT flagging, but the script exits 0 regardless. Default (no flag) behavior is completely unchanged — verified both modes directly: no flag still exits 1 with the same blocking report; `--allow-drafts` exits 0 with a "NOTICE" (not "BLOCKING") report instead.

**Dockerfile** — two-stage build. Stage 1 (`node:20-alpine`) runs `build-home.mjs`, `build-noscript.mjs`, then `build-case-pages.mjs --allow-drafts` against a copy of the repo — all three are pure Node with zero npm dependencies (confirmed by grepping every import in all three: only `node:fs`/`node:path`/`node:url` and the project's own `data/cases.js`/`js/case-utils.js`), so no `npm install` step exists or is needed. Stage 2 (`caddy:2-alpine`) copies stage 1's output and serves it. `build-og-images.mjs` deliberately does **not** run in this build — it needs Playwright, which this project has never made a dependency; OG images stay pre-generated, committed, manually regenerated only when case content changes enough to need new ones (documented in the script's own header already).

**Caddyfile** — listens on `:{$PORT}` (Railway-injected). Clean URLs (`/work/some-slug/` → its `index.html`) rely on Caddy's own documented `file_server` behavior (directory→index resolution, automatic trailing-slash redirect) rather than a hand-rolled rewrite rule. Cache headers: `public, max-age=31536000, immutable` for `.woff2`/`.png`/`.jpg`/`.jpeg`/`.webp`/`.svg` (fonts and OG images, which only ever change by getting a new filename); `no-cache` for `.html`/`.css`/`.js` — extended the "short for HTML" ask to CSS/JS too, since neither has a content hash in its filename and a long cache on either would mean a visitor's browser keeps serving a stale stylesheet or script after a deploy fixes something. `handle_errors { rewrite * /404.html; file_server }` is Caddy's own documented idiom for a custom error page that keeps the real 404 status code, not a 200.

**404.html** — a real page in the site's own system (same header/footer, same pastel design language), not Caddy's bare default error text. Written with absolute paths (`/css/...`, `/fonts/...`, not `../css/...`) deliberately — Caddy's error-handling rewrite is server-side, so the browser still believes it's at the original mistyped URL, and a relative path would resolve against *that* depth, not this file's real location.

**railway.json** — points Railway at the Dockerfile explicitly (`build.builder: "DOCKERFILE"`), plus a conservative restart policy. Minimal on purpose — no env vars needed (no database, no secrets, `PORT` is injected automatically).

**README.md** — didn't exist before this turn; created one covering local dev, when and how to re-run the three generator scripts (and why `build-og-images.mjs` is separate), and the exact numbered Railway steps: push → New Project → Deploy from GitHub repo → Railway auto-detects `railway.json`/`Dockerfile` → no manual build/start command or env vars needed → verify the deployed URL (home, `/work`, a case page, a deliberately-wrong URL for the 404 page) → optional custom domain → every subsequent push auto-redeploys.

**`.gitignore`/`.dockerignore`:** `.gitignore` already correctly excluded `.DS_Store` and `node_modules/` and needed no changes — confirmed by searching for any env/secret/credential file at the repo root or one level down (none exist) and checking `git status` after regenerating everything (no unexpected untracked files). Added `.dockerignore` new this turn (didn't exist before) to keep `.git`, `tasks/`, and every `*.md` (this README included) out of the served image — none of that is site content, and Docker's build context isn't filtered by `.gitignore` the way a git operation would be, so without this the container would have shipped internal process docs and local junk files like `.DS_Store` as network-reachable static files.

**Honest limitation:** neither Docker nor Caddy is available in this environment, so the Dockerfile and Caddyfile could not be built or run end-to-end here — everything above is verified as far as this environment allows (build scripts run correctly in the exact sequence and flag combination the Dockerfile uses; every case directory has an `index.html`; `404.html` exists with correct absolute paths) but the container itself is unverified. Recommend either testing with Docker Desktop locally before connecting Railway, or watching the first Railway build/deploy log closely, since that will be the first real test of this exact Dockerfile and Caddyfile.

**Still outstanding, unchanged:** the CV PDF doesn't exist — flagged again in the README's own deploy section, directly in the path of "before sharing the domain with anyone," since that's now literally true the moment a Railway URL exists. `assets/portrait.jpg` is also still a placeholder.

## Review — Job 2 revised: deploy target switched to Cloudflare Pages

Railway's free tier is capped (two project slots, both already used elsewhere) — deploy target changed to Cloudflare Pages. Instructed to keep the Dockerfile/Caddyfile/`railway.json` in the repo rather than remove them (unused costs nothing, Railway may come back into play later), and to prepare the Cloudflare equivalent of everything from Job 2. Nothing was connected or deployed here either — same standing instruction as Railway.

**Build command and output directory are dashboard fields for Cloudflare Pages, not a repo config file** — a git-connected Pages project reads its build command/output directory from project settings entered once when the project is created, not from something like a `wrangler.toml`. Rather than write a config file whose applicability to a git-integration build I wasn't fully certain of, documented the exact values to type into the dashboard instead: build command `node scripts/build-home.mjs && node scripts/build-noscript.mjs && node scripts/build-case-pages.mjs --allow-drafts`, output directory `/` (the three scripts rewrite `index.html`/`work/index.html`/every `work/{slug}/index.html` in place — there's no separate build output folder for this site, the built result *is* the repo root).

**`.nvmrc`** (new) pins Node to `20`, matching the Dockerfile's `node:20-alpine` — Cloudflare Pages reads this automatically for a git-connected build; documented `NODE_VERSION=20` as the dashboard-side fallback if it's ever not picked up.

**`_headers`** (new) — Cloudflare Pages' own header-rules file, syntax compatible with Netlify's `_headers` (which Cloudflare's format is explicitly modelled on). Three rules, not six: `*.woff2` and `assets/og/*` get `public, max-age=31536000, immutable`; a single broad `/*` catch-all gets `no-cache` for everything else. Chose the catch-all deliberately over enumerating `*.html`/`*.css`/`*.js` separately — this site's real URLs are almost all clean, directory-style paths (`/work/some-slug/`) that never literally end in `.html`, so a pattern matching only the `.html` extension would miss the pages that actually get requested; a "no-cache by default, two explicit immutable exceptions" design is both simpler and correctly covers every clean URL without needing to enumerate them. Flagged explicitly, in a comment in the file and in the README, that Cloudflare's precedence for competing glob patterns (specific-vs-broad) couldn't be verified live from this environment — ordered the specific rules first, which is correct under either of the two precedence models both Cloudflare's and Netlify's own docs describe (most-specific-wins, or first-match-top-to-bottom), and recommended a direct `curl -I` check after the first deploy as the real verification.

**`404.html` needed no changes at all** — it already used absolute asset paths (`/css/...`, not `../css/...`), which turns out to be exactly what Cloudflare Pages also needs for the same reason Caddy did: the error page is served in place at whatever URL was actually requested, so a relative path would resolve against that URL's depth rather than the file's real location at the repo root. One file, correct for both platforms simultaneously, without change.

**Clean URLs and the 404 page both rely entirely on Cloudflare Pages' own default conventions** — a root-level `404.html` is automatically served (with a real 404 status) for any unmatched path, and a directory-style request resolves to that directory's `index.html` automatically, for any static Pages deployment. Nothing extra to configure for either.

**README** restructured: Cloudflare Pages is now the primary "Deploying to..." section with the numbered dashboard steps above; the Railway section moved under "Alternative deploy target (prepared, not currently used)" with its own honest unverified-here caveat carried forward unchanged, rather than deleted.

**Same honest limitation as the Railway config, worse in one respect:** this environment has neither Docker/Caddy (as before) NOR any way to run or simulate Cloudflare Pages, so the `_headers` precedence behaviour and the exact clean-URL/404 conventions are documented from Cloudflare's own published behaviour, not verified against a live deployment. Re-ran the actual build script sequence (`build-home.mjs`, `build-noscript.mjs`, `build-case-pages.mjs --allow-drafts`) to confirm it still exits 0 and produces no drift in the generated files — that part is real and checked; the platform-specific pieces (header precedence, URL resolution) are not, and the README says so directly rather than implying they were.

## Review — Job 2 revised again: deploy target switched to GitHub Pages

Cloudflare Pages also became unavailable — deploy target changes to GitHub Pages, as a user site at `lagracie0.github.io` (the repo is being renamed to match, separately from anything here). Instructed to keep Railway's config AND Cloudflare's `_headers` in the repo as documented-unused alternatives, add the Actions workflow, add `.nojekyll`, confirm nothing relies on `_headers` and say plainly if caching gets worse, verify `404.html` against GitHub Pages' conventions, and update the README. Nothing enabled in repo settings — that's explicitly left to Ayomide.

**`.github/workflows/deploy.yml`** — triggers on push to `main` plus `workflow_dispatch`, exactly as specified (no extra triggers added). `permissions: pages: write, id-token: write` (plus `contents: read` for checkout) at the workflow level. Node pinned via `actions/setup-node`'s `node-version-file: '.nvmrc'` rather than hardcoding `20` a second time in the workflow, so the two files can't drift against each other. Runs the same three scripts and `--allow-drafts` flag every other prepared target uses. Publishes via `actions/upload-pages-artifact` + `actions/deploy-pages`, in a `github-pages` environment, with a `concurrency` group so a fast second push queues rather than cancels a deploy mid-flight. Validated the YAML by actually parsing it (Python's `yaml.safe_load`), not just eyeballing it — confirmed both jobs, the exact permission set, and both triggers are present as written.

**No repository or environment secrets exist for this workflow at all** — `deploy-pages` authenticates via the OIDC token from `id-token: write`, not a stored secret. Documented in a workflow comment and the README that *if* one is ever needed later, it should be a repository secret, not an environment secret, per instruction — there's nothing concrete to add today since nothing needs one.

**A real problem found and solved before it could ship:** GitHub Pages runs Jekyll by default, which normally auto-excludes any `_`/`.`-prefixed file or directory from what's published — exactly the behaviour `.nojekyll` (added, empty file at repo root, per instruction) turns off. But turning it off means NOTHING is auto-excluded anymore, and this repo's root has real internal files sitting right next to the site: `CLAUDE.md`, the WRD doc, `tasks/todo.md` (a very long internal build log), and every other platform's deploy config (`Dockerfile`, `Caddyfile`, `railway.json`, `_headers`). Without `.nojekyll`, Jekyll would have accidentally protected `_headers` specifically (by dropping it) while still leaking everything else that doesn't start with `_`/`.`. Solved by adding a staging step to the workflow that `rsync`s a clean copy of the site into a temporary `_site/` directory, explicitly excluding all of the above, and publishing *that* instead of the raw repo root. Verified this exclusion list directly against the real repo — ran the actual `rsync` command locally (matching the workflow step exactly) and confirmed: `.nojekyll`, `404.html`, `index.html`, and all 12 case pages present in the staged output; `CLAUDE.md`, the WRD doc, `tasks/`, `scripts/`, `Dockerfile`, `Caddyfile`, `railway.json`, `_headers`, `.nvmrc`, and `.github` all correctly absent.

**Said plainly, per instruction, not implied:** GitHub Pages does not read `_headers` at all — there's no GitHub Pages equivalent for custom cache headers, full stop, and this repo's configuration can't close that gap. Fonts and OG images lose the `public, max-age=31536000, immutable` treatment they'd get under Cloudflare Pages or Railway/Caddy; GitHub Pages applies its own uniform caching to everything, shorter than a year and not distinguishing file types. Stated the real-world consequence directly in the README rather than leaving it abstract: no difference for a first-time visitor, but a returning visitor's browser re-fetches fonts/OG images more often than under either other target. `_headers` itself is excluded from what actually gets published on this deploy anyway (see the staging step above), so this isn't even a "the file is present but ignored" situation — it genuinely isn't there.

**`404.html` needed no changes, and this time I can say why with real confidence, not just by analogy.** GitHub Pages' automatic root-level-404.html convention (served with a real 404 status) is long-standing, well-documented platform behaviour, not a newer or less-certain feature the way some of Cloudflare's specifics were last turn. The file's existing absolute paths (`/css/...`, not `../css/...`) are exactly correct here specifically *because* `lagracie0.github.io` is a **user site**, served at the true domain root rather than under a project subpath (`lagracie0.github.io/reponame/`) — had this been a project-page deployment instead, those same absolute paths would have been wrong. Confirmed this is genuinely a user-site deployment (the repo being renamed to match is the actual mechanism that makes it one) rather than assuming it.

**README** restructured again: GitHub Pages is now the primary section (with the Jekyll/`.nojekyll`/staging explanation and the plain caching-loss statement inline, not just cross-referenced), Cloudflare Pages and Railway both moved under one combined "Alternative deploy targets (prepared, not currently used)" section, each caveat carried forward rather than dropped.

**Verified, same pattern as every deploy turn:** re-ran the three build scripts to confirm zero drift in generated files, and this time additionally ran the actual staging/exclusion logic the workflow uses (not just the build) directly against the real repo, since that logic could be checked in this environment even though the GitHub Actions runner itself, and GitHub Pages' live serving behaviour, could not.

## Review — PRD v2 Phase 1: adaptive case templates, all cases rewritten

PRD v2 supersedes the WRD. This turn is **Phase 1 only** (assign a template to every case; rewrite them all to the §6 standard), per the PRD's own phasing and this file's standing "stop for review after each step" convention. Phases 2–4 (visual propagation, responsive pass, launch blockers) are not started.

**Two gaps in the PRD's template assignments, asked rather than guessed.** Template C's list named "the client delivery infrastructure work", which matches no case in the data — Lodgr and Boldtron, the only cases carrying that CV bullet, were already assigned to Template B in the same document. And Langovest website redesign was not named under any template at all, while every other case was. Both resolved by Ayomide before any rewriting started: the first is the existing shared bullet (no new case), the second is Template B. Guessing either would have meant redoing real work.

**Final assignment (12 cases):** A/Live operation — Situation Room, AIR, Osun Tech Festival. B/Product delivery — Lodgr, Boldtron, Clea & Pushbio, Calnita, Langovest redesign. C/Built from nothing — Volunteer Network, Frobits. D/Ongoing function — Lendsqr, FMC.

**Data model rebuilt around the templates.** `sections: [{heading, body: [...]}]` replaces the fixed six blocks, so each case carries only the headings its template defines and empty blocks are structurally impossible. A new `TEMPLATES` export holds each template's exact ordered headings, and `build-case-pages.mjs` validates every listed case against them — a case can't drift into a shape nothing else expects. `scaleLine` (Template A) and `contextLine` (Template B) carry the date/scale and stack/team-shape lines those templates call for; both omitted rather than invented where the CV is silent. `metrics[].method` is now optional per §5 (v1 made it mandatory), and metrics are no longer rendered on case pages at all — the numbers already appear inside the prose; the data is kept for the stats block contemplated in §12 Q4.

**Eleven cases rewritten as prose, first person, past tense.** Substantial rather than clipped, per instruction — most run 4–8 paragraphs. AIR uses Ayomide's own worked example from §6 verbatim, since that was given as the standard. Everything else traces to the CV, to the confidentiality-cleared C1 content, or to the About page's own account of the FMC chairman.

**Frobits is delisted, not written.** It is Ayomide's own product and appears nowhere in the CV — every block would have been invention. PRD §5: a case is either written or it is not listed. It is skipped entirely (no page, no card, no link), its stale generated page deleted, with a comment in the data recording exactly what's needed to list it. This is also what resolves launch blocker 3: there are no draft banners in production because there are no drafts — the mechanism is delisting, not flagging.

**Retired with the v1 schema, because nothing emits them any more:** `[NEEDS INPUT]` rendering, `.draft-banner`, and the RAG outcome badges (colour now keys to engagement type, not status — §7). Worth naming plainly: this reverses an explicit earlier instruction to keep NEEDS-INPUT/draft styling loud. The PRD supersedes it, and the replacement rule is stricter rather than looser — nothing provisional reaches the public site at all now.

**A `LISTED_CASES` export was added and every consumer repointed at it** (`render-work.js`, `build-noscript.mjs`, `build-home.mjs`). An unlisted case has no page, so any consumer that forgot to filter would produce a dead link — this makes that structurally hard rather than a thing to remember. v1's P0/P1 ordering was dropped with the launch-list concept; ordering is now recency alone, in both the runtime and the build-time copy of that logic.

**Also done, because leaving them half-applied would have broken the site:** `/method` cut (§4) — directory, OG image, CSS rules and every nav link across five pages — and the §1 correction from "product" language to **Project & Delivery Manager** in the home page title, meta, OG tags, hero eyebrow and both JSON-LD blocks.

**A real, deterministic CLS bug found and fixed — present on every page, not introduced by this work.** At 375–499px the mono nav links fit on one line in the metric-matched fallback but wrap to two once real JetBrains Mono loads, taking the header from 53.2px to 85.2px and pushing the whole page down 32px: a reproducible ~0.19 CLS, identical across three separate case pages, so not a race and not page-specific. Fixed by reserving the taller height in exactly that width band (a no-op below 321px where both states already wrap, and scoped out at 500px+ where neither does), plus `align-content: flex-start` so the reserved box doesn't re-centre its flex lines and move the nav by half the delta anyway. The second half of it: `/work` and every case page preloaded **zero** fonts while every other page preloaded five — a perfect correlation with which pages shifted. Preloads added to the case-page template and `/work`.

**Result: CLS 0.0000 on home, /about, /contact, /404 and all eleven case pages, verified twice each. Lighthouse ≥95 on all four categories everywhere** (most pages 100). Keyboard order clean with every stop focus-visible; readable with JS disabled (home 3 featured cards, /work all 11, case pages full prose); no horizontal overflow at 375px.

**One constraint not met, stated plainly: `/work` is at CLS 0.079, not 0.** Cause is understood exactly — it is the one page whose content is still built by JS after first paint (`render-work.js` fills an empty `#work-app`, and the chip row it inserts above the list pushes the list down), the same class of bug the landing page had before it went static. The fix is to promote the already-generated card list out of `<noscript>` into the real DOM so it is present at first paint, with JS taking over from there, and to pre-render or reserve the chip row. That touches the filter machinery, and `/work`'s cards are being reworked in Phase 2 anyway (§7: colour by type, tags demoted), so it is scheduled there rather than rushed at the end of this turn. It is below the 0.1 "good" threshold but it is not the zero the PRD asks for.

**Open, and blocking content rather than code:** §12's four questions are all still open. One is worth flagging specifically — §12 Q4 lists "70+ agents coordinated" among the numbers to confirm for a stats block. That figure appears nowhere in the CV or in any case data, so it is not used anywhere in these rewrites; Situation Room's scale line carries only what is confirmed (dates, a cluster of LGA desks, Osun State). The same applies to images (§12 Q1) — no case has one, and the PRD asks for a considered typographic treatment rather than an empty box where none exists, which is what Phase 2 will need to decide.

**Launch blockers unchanged:** CV PDF still 404s from every Download CV link; the portrait is still a placeholder; Situation Room clearance is still unconfirmed. Blocker 3 (draft cases) is resolved by the delisting rule above.

## Review — PRD v2 Phase 2 (part 1): warm palette, case pages, /work

Palette approved before any CSS was written, per instruction. `/about` and `/contact` held back for review as asked — they inherit the warm neutrals site-wide (which was the explicit instruction: don't half-apply it) but keep their phase-1 layout until the next round.

**Why the palette could finally be warm.** It read cold for a structural reason, not a stylistic one: red, amber and green were fenced off for RAG status meaning, so every warm hue was unusable and only blue/violet were left. Phase 1 retired RAG with the outcome block that carried it. Verified the tokens were genuinely unused before proposing anything.

**Six values, all measured rather than estimated:** warm cream paper `#F6F2EA`, warm near-black ink `#1B1714` (deliberately not pure black, per the condition), warm grey line `#6B6259`, and four engagement-type hues — terracotta `#A8442B` (Live operation), ochre `#7F5A0D` (Product delivery), moss `#4F6134` (Built from nothing), plum `#7A3F55` (Ongoing function), each paired with a tint. Every combination clears WCAG AA including the awkward deep-on-tint one; the first ochre candidate failed it at 4.46 and was darkened rather than shipped. Minimum hue separation 29°, so the four stay tellable apart at small sizes.

**Colour has real presence, not just tinted labels** (condition 1): each case page opens with a full-width band filled in its type's tint, and every `/work` card is a tint fill of its type. The category is legible before a word is read.

**Kept deliberately distinct from the rejected cream-and-serif-and-terracotta look** (condition 2): four hues carrying distinct meaning rather than one decorative accent, warm near-black rather than pure black, and Archivo for section headings rather than an editorial serif — which also settles the earlier "serif or display" question in favour of display.

**Case pages rebuilt.** The prose was pinned to a narrow left column with the right half of a laptop screen empty; it is now a type-coloured header band across the full width, then a two-column layout — a sticky side column (numbered section navigation, field, capabilities) beside the prose, centred as a group, collapsing to one column below 1100px. The title moved onto its own smaller clamp (tops out at 40px instead of 48px) so type, title, byline and the opening paragraph land together. Mono uppercase section headings are gone, replaced by Archivo at 1.3rem.

**/work rebuilt.** Grouped by engagement type rather than led by filter rows, cards filled in their type's tint, capability tags removed from cards entirely — six identical mono chips were the first thing a visitor read, and colour does that job better. Filters remain but sit below the header. Three cards across at 1100px+ rather than two, because two at 1280 leaves them uncomfortably wide.

**The scheduled `/work` CLS fix is done, and it went further than planned.** `scripts/build-work.mjs` now pre-renders both the chip row and the grouped card list as real DOM; `js/render-work.js` no longer builds anything, it attaches to what exists and filters by hiding. That removes the last after-paint injection on the site, and it also removed the `<noscript>` duplicate entirely — a JS-disabled visitor now gets the same 11 cards in the same 4 groups as everyone else, with no second copy to keep in sync. `scripts/build-noscript.mjs` was deleted as superseded.

**Verified:** CLS **0.0000 on every page including /work**, twice each. Lighthouse 99–100 across all four categories on every page checked. No horizontal overflow at 375, 768, 1280 or 1440. Chips are keyboard-reachable with visible focus, activate on Enter, update `aria-pressed`, keep focus (nothing is rebuilt, so there is no focus to restore) and sync the URL. Page weight 137–151 KB against the 500 KB budget.

**Corrected in passing:** the `/work` header claimed twelve engagements; eleven are listed since Frobits was delisted.

**Still open:** `/about` and `/contact` await review before being moved onto the type system. Launch blockers unchanged — CV PDF, portrait photo, Situation Room clearance.
